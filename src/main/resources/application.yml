server:
  port: 8089

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/plazoletaservice
    username: root
    password: sasa
  application:
    name: plazoleta-service

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Pragma Powerup"
appVersion: "1.0.0"
